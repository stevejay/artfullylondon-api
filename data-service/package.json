{
  "name": "artfullylondon-api-data-service",
  "license": "MIT",
  "scripts": {
    "test": "cross-env NODE_ENV=test mocha ./lib/**/*.tests.js",
    "posttest": "yarn run lint",
    "lint": "eslint .",
    "deploy": "yarn test && node build.js && sls deploy --stage=production -v",
    "offline": "sls offline start --stage=development --port 3010 --noTimeout"
  },
  "devDependencies": {
    "cross-env": "^5.1.5",
    "eslint": "^4.19.1",
    "loglevel": "^1.6.1",
    "mocha": "^5.1.1",
    "serverless": "^1.27.2",
    "serverless-offline": "^3.23.0",
    "serverless-webpack": "^5.1.5",
    "should": "^13.2.1",
    "sinon": "^5.0.7",
    "webpack": "^4.8.3",
    "webpack-node-externals": "^1.7.2"
  },
  "dependencies": {
    "elasticsearch": "^15.0.0",
    "lodash.padstart": "^4.6.1"
  }
}
