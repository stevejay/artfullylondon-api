{
  "name": "artfullylondon-api-tag-service",
  "license": "MIT",
  "scripts": {
    "test": "cross-env NODE_ENV=test jest ./src",
    "posttest": "yarn run lint",
    "test:integration": "cross-env NODE_ENV=test jest ./tests/integration",
    "lint": "eslint .",
    "deploy": "yarn test && node build.js && sls deploy --stage=production -v",
    "offline": "sls offline start --stage=development --port 3011 --noTimeout"
  },
  "devDependencies": {
    "aws-sdk": "^2.241.1",
    "cross-env": "^5.1.5",
    "eslint": "^4.19.1",
    "jest": "^22.4.3",
    "request": "^2.86.0",
    "request-promise-native": "^1.0.5",
    "serverless": "^1.27.2",
    "serverless-dynamodb-local": "^0.2.30",
    "serverless-offline": "^3.23.0",
    "serverless-webpack": "^5.1.5",
    "webpack": "^4.8.3",
    "webpack-node-externals": "^1.7.2"
  },
  "dependencies": {
    "dynamodb-doc-client-wrapper": "^1.3.3",
    "ensure-request": "^4.0.0",
    "jwt-decode": "^2.2.0",
    "lambda-generator-handler": "^1.0.1",
    "limax": "^1.6.0",
    "lodash.groupby": "^4.6.0",
    "normalise-request": "^1.4.4"
  }
}
