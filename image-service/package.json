{
  "name": "artfullylondon-api-image-service",
  "license": "MIT",
  "scripts": {
    "test": "cross-env NODE_ENV=test jest ./src",
    "posttest": "yarn run lint",
    "test:integration": "cross-env NODE_ENV=test jest ./tests/integration",
    "lint": "eslint .",
    "deploy": "yarn test && sls deploy --stage=production -v",
    "local":
      "cross-env sls offline start --stage=development --port 3020 --noTimeout",
    "local:install": "sls dynamodb install",
    "local:dynamodb": "node ./scripts/start-dynamodb-local.js"
  },
  "devDependencies": {
    "aws-sdk": "^2.247.1",
    "cross-env": "^5.1.6",
    "dynamodb-localhost": "^0.0.5",
    "eslint": "^4.19.1",
    "jest": "^23.0.1",
    "serverless": "^1.27.3",
    "serverless-dynamodb-local": "^0.2.30",
    "serverless-offline": "^3.24.4",
    "serverless-offline-sns": "^0.45.0",
    "serverless-webpack": "^5.1.5",
    "webpack": "^4.10.1",
    "webpack-node-externals": "^1.7.2"
  },
  "dependencies": {
    "delay": "^3.0.0",
    "dominant-color": "0.0.1",
    "dynamodb-doc-client-wrapper": "^2.0.1",
    "ensure-request": "^4.0.0",
    "imagemagick": "^0.1.3",
    "jwt-decode": "^2.2.0",
    "lambda-error-handler": "^1.0.0",
    "lodash-uuid": "^0.0.3",
    "loglevel": "^1.6.1",
    "normalise-request": "^1.4.4",
    "request": "^2.87.0",
    "request-promise-native": "^1.0.5",
    "serverless-dynamodb-client": "^0.0.2",
    "tmp": "^0.0.33"
  }
}
